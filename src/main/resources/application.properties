# CONEXION CON LA BASE DE DATOS MYSQL
spring.datasource.url=jdbc:mysql://localhost:3306/Clientes
# Nombre del usuario de la base de datos
spring.datasource.username=root
# Contrase�a de la base de datos
spring.datasource.password=
# Elecci�n del driver con el que quiero trabajar
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
# Visualizar consultas
spring.jpa.show-sql=true
# Actualizar el esquema, cada vez que ejecuto
# ddl (Lenguaje de descripci�n de datos)
# auto update (si cambio un cambio, se va a realizar un alter)
# spring.jpa.hibernate.ddl-auto=update
# create-drop nos permite borrar la tabla y volverla a crear con informaci�n
spring.jpa.hibernate.ddl-auto=create-drop
# Puerto del servidor
server.port=8081